import styled from 'styled-components';

export const StyledPhotoGridItem = styled.div`
   position: relative; 
   margin-bottom: 1rem;
   break-inside: avoid;
`

export const StyledImg = styled.img`
    width: 100%;
`;